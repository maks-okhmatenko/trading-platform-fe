import * as React from 'react';

import CurrencyDetails from '../../components/ui/CurrencyDetails';

const CurrencyDetailsWidgetContainer = (props) => {

  return (
    <CurrencyDetails/>
  );
};

export default CurrencyDetailsWidgetContainer;
